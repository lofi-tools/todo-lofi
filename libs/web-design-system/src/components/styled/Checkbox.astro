---
import { Checkbox } from './interactive'

interface Props {
  label: string
  defaultChecked?: boolean
  disabled?: boolean
  class?: string
}

const { label, defaultChecked, disabled, class: className } = Astro.props
---

<Checkbox label={label} defaultChecked={defaultChecked} disabled={disabled} class={className} client:only="solid" />
