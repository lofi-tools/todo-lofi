---
import { Accordion } from './interactive'

interface AccordionItem {
  value: string
  trigger: string
  content: string
}

interface Props {
  items: AccordionItem[]
  class?: string
}

const { items, class: className } = Astro.props
---

<Accordion items={items} class={className} client:only="solid" />
